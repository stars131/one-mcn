# One MCN Project Rules

## 最高标准

- 现在开始不只是做 MVP，直接按照最优秀的方案进行。
- 任何修改必须先提出至少两种可行的方案供用户选择；用户选择后再开始实现。

## 对话式采集标准

- 对话式采集必须像和 GPT 对话一样逐轮推进。
- 系统要基于用户当前输入产出可选择的选项，让用户通过点击、选择、少量补充来继续。
- 用户选择的选项必须累积进对应资料或内容上下文，而不是只作为一次性前端状态。
- 每轮对话都应该尽量减少空白输入，让用户从推荐选项、方向、平台、内容类型、风格、目标和边界中选择。
- 采集结果应调用真实大模型，并通过清晰提示词和结构化输出沉淀到业务数据。

## 产品原则

- 面向小白用户，优先提供按钮、选项、推荐方案和对话式流程。
- 默认界面保持简约，复杂配置通过切换、折叠、弹窗或分步流程承载。
- 对话页面必须像 Apple 公司的产品一样简约：首屏只保留核心对话和下一步动作，状态摘要轻量呈现，资料、配置、历史和高级信息默认折叠或渐进展开。
- 平台能力先统一注册和预留接口，再逐个平台维护细节。
- 不能让用户手动填写 JSON 或复杂表单，除非该页面明确是内部管理或调试用途。
